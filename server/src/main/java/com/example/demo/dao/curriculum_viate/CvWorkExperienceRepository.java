package com.example.demo.dao.curriculum_viate;

import com.example.demo.model.resume.RWorkExperience;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CvWorkExperienceRepository extends JpaRepository<RWorkExperience,String> {
}
